<h1>New Song Form</h1>

<%= form_for(@song) do |s| %>
    <%= s.label :name %>
    <%= s.text_field :name %><br>

    <%= s.label :artist_id %>
    <%= s.number_field :artist_id %><br>

    <%= s.label :genre_id %>
    <%= s.number_field :genre_id %><br>

    <%= s.submit %>

<% end %>